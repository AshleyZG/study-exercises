{
  "version": "0.2.0",
  "configurations": [
    {
      // F5 runs the open file in the terminal.
      "name": "Run this file",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
